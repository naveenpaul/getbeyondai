{
  "name": "@getbeyond/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared types + contracts (adapter interfaces, draft schemas). MIT — embedders can build closed-source adapters against this surface.",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./connector-contracts": "./src/connector-contracts.ts"
  },
  "scripts": {
    "lint": "eslint \"src/**/*.ts\"",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
